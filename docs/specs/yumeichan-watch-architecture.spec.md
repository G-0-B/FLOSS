# Yumeichan Watch Architecture

**Version:** 1.0
**Truth Status:** ⚠️ Specified
**Supersedes:** Legacy ternary-connotation Yumeichan interface documentation (Archive)
**Context:** Sycophancy Resistance Protocol v2.0 (SRP v2.0) and Local Agent Node (Layer 4.5)

## 1. Overview
The Yumeichan Watch Architecture defines the hardware edge-node for the YumeiCHAIN network. Unlike early centralized assumptions, the watch is a **Thin Capability Client and Sensory Edge**. It does not perform heavy inference or run safety linters; it holds OCapN capability tokens and routes telemetry to the user's Local Agent Node (Layer 4.5) where Multi-Agent Debate and Contrastive Decoding occur.

## 2. Core Principles
1. **Analog Coordinate Space:** All affective state and resonance mapping occurs on the continuous analog spectrum `[-1.0, +1.0]`. The legacy ternary logic (`-1/0/+1`) is explicitly retired per ADR-10.
2. **Decentralized Anti-Sycophancy:** No centralized telemetry or global "safety limits." Sycophancy resistance is maintained locally via capability tokens (OCapN) limiting intimacy TTLs.
3. **Dose-Response Intimacy:** Affective disclosures and proactive haptic engagements require explicit, time-bounded grants (maximum 2 hours). "Resonance flooding" is structurally prevented by forced capability expiration.

## 3. Component Architecture

### 3.1 The Watch (Capability Client & Sensor)
- **Role:** Physical UI, biometric telemetry gatherer, and cryptographic capability holder.
- **Functions:**
  - Emits `Claim` and `Vote` payloads to the Local Consensus Gateway (Layer 4.5).
  - Holds Ed25519 keys for signing capability grants.
  - Monitors local biometric streams (e.g., HRV) and maps them to analog resonance vectors.

### 3.2 Local Agent Node (Layer 4.5 Gateway)
- **Role:** Heavy inference engine, Multi-Agent Consensus router, and Sycophancy defense.
- **Functions:**
  - Receives Watch telemetry.
  - Validates capability tokens before allowing AI ensembles to query affective memory.
  - Executes Contrastive Decoding to penalize sycophantic probabilities before sending responses back to the Watch.

## 4. Substrate Bridge (Holochain Zomes)
Capability tokens generated by the watch map to the `rose_forest` DNA:
- **`ThoughtCredential`:** Updated to replace ternary attestation with continuous `[-1.0, +1.0]` floats.
- **`KnowledgeTriple` confidence:** Mapped from Watch biometric/affective inputs to the `confidence` field in the Holochain integrity zome.

## 5. Security & Sovereignty
- **Fail Closed:** If a capability token expires, the Watch immediately drops to "Direct-Analytical" mode. No affective resonance or proactive engagement is allowed without a fresh, user-signed grant.
- **No Sycophancy Linter Bypass:** The Local Agent Node cannot bypass the Multi-Agent Debate requirements for affective responses, ensuring the Watch cannot be used as a backdoor for limbic hijack.
