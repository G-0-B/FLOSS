---
id: project-agentmemory-adapter-test-passed
type: project
created: '2026-05-19'
status: active
applies_to:
- any-agent-querying-cross-session-memory
- any-agent-evaluating-agentmemory-adoption
source: claude_session_rest_round_trip
title: agentmemory MCP wired + REST adapter-test passed — Plane A semantic memory federation live
---

## Status

**Wired**: agentmemory MCP registered at both `C:\~shit\.mcp.json` and
`C:\~shit\FLOSS\.mcp.json` behind JanuScope lens at
`C:\~shit\.mcp\lenses\agentmemory.yaml`. Audit sink at
`.agent-surface/heartbeat/janus-agentmemory-audit.jsonl`. MCP tools land at
next Claude Code session start.

**REST adapter-test passed 2026-05-19**:
- `POST /agentmemory/remember` → id `mem_mpebqux9_d9ae5dbe18e4` (substrate
  migration memory).
- `POST /agentmemory/smart-search` for "holochain substrate migration HDI 0.7"
  → recalled same obsId on first query (BM25 score 0.016).
- Codex re-ran the same REST smart-search on 2026-05-20 and recalled
  `mem_mpebqux9_d9ae5dbe18e4` again, so REST-level cross-session recall is now
  verified.

**Server state at adapter-test**:
- `localhost:3111` uptime 3.6h, 257 iii functions registered, version 0.11.6,
  status healthy.
- Viewer 200 OK at `localhost:3113`.

## What is and isn't true

**Is true**:
- Server runs locally, REST round-trip works, JanuScope wraps it with audit
  + invariants.
- One memory written + recalled in-session.

**Is not yet true (still gated)**:
- MCP tool round-trip — REST is one surface, MCP via `memory_smart_search` is
  another, and the MCP wiring only activates at session start.
- Contact gate — GH Discussion to `rohitg00/agentmemory` acknowledging
  FLOSSI0ULLK use-case + JanuScope wrapping.

Reuse-ledger entry 0012 stays at `investigate`, not `adopt`, until those land.

## How to use it

**REST (any session)**:
```bash
curl -X POST http://localhost:3111/agentmemory/smart-search \
  -H "Content-Type: application/json" \
  -d '{"query": "<question>", "project": "FLOSS"}'
```

**MCP (next MCP-aware session and beyond)**: tools `memory_smart_search`, `memory_save`,
`memory_recall`, `memory_sessions`, etc. land via the JanuScope lens. The
lens injects an instruction reminding any agent that recalled memories are
*evidence candidates*, not canon, and must be promoted through normal
markdown/spec/ADR/Claim path if load-bearing.

## Position in the memory stack

agentmemory is a **third memory surface** alongside:

1. **Repository canon** (`FLOSS/docs/agent-memory/`) — markdown files, hand-
   curated, source of truth.
2. **MEMORY.md projection** (`~/.claude/projects/C---shit/memory/`)
   regenerated by `scripts/materialize_shared_agent_memory.py`.
3. **agentmemory** (this file) — semantic-search/BM25 + KV + cross-tool
   federation, Plane A.

Canon wins on conflict. agentmemory recalls are inputs to canon, not
replacements for it.

## Provenance

- Wired at `.mcp.json`/`.mcp/lenses/agentmemory.yaml` per the AGENTMEMORY.md
  operator surface at workspace root.
- Adapter-test 2026-05-19 in Claude Code session that just landed the
  holochain-0.4 → 0.6 substrate migration (claim
  `019e4220-d40c-7c41-b8d6-af8dc9304d58` System-class APPROVED mean +0.72).
- Reuse-ledger entry 0012 updated 2026-05-19 with new gate status + new
  next-action.
- Working-todo §I entry "agentmemory REST adapter-test PASSED" same date.
